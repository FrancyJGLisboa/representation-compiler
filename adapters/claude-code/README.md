# Claude Code adapter

This adapter exposes the canonical skill as `/representation-compiler` after it is installed as a Claude Code plugin. The actual instructions remain in `skills/representation-compiler/` at the repository root.

Use it with:

```text
/representation-compiler

I want to understand this material:
[paste a transcript, URL, PDF, or notes]
```
